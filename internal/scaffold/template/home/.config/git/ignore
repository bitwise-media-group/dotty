# ignore commit.sh
commit.sh

# local claude settings
/.claude/settings.local.json
/CLAUDE.local.md
/.claude/plans

# macos files
.DS_Store

# env files and per-machine fnox overrides
.env.*
fnox.local.toml

**/.claude/.cc-writes/
{{- if not .WorktreesAbs }}

# agent worktrees live inside each repository
{{.Worktrees}}/
{{- end }}
