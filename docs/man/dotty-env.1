.nh
.TH "DOTTY" "1" "Jun 2026" "Auto generated by spf13/cobra" ""

.SH NAME
dotty-env - Store and inject credentials from the macOS Keychain.


.SH SYNOPSIS
\fBdotty env  [flags]\fP


.SH DESCRIPTION
Manage generic credentials in the macOS login keychain and inject them into
templates and processes — the way the 1Password CLI does, but with no external
service. Secrets are grouped into namespaces; each namespace is a single
keychain item under the service name "dotty:". get reads one value
(like op read), use fills a template (like op inject), and run launches a
process with the namespace's secrets in its environment (like op run).


.SH OPTIONS
\fB-h\fP, \fB--help\fP[=false]
	help for env

.PP
\fB--namespace\fP="default"
	credential namespace to operate on


.SH OPTIONS INHERITED FROM PARENT COMMANDS
\fB--profile\fP=""
	profile to operate on (defaults to the active profile)


.SH EXAMPLE
.EX
  dotty env add --namespace=aws AWS_ACCESS_KEY_ID
  dotty env list --namespace=aws
  dotty env get --namespace=aws AWS_ACCESS_KEY_ID
  dotty env run --namespace=aws -- aws s3 ls
.EE


.SH SEE ALSO
\fBdotty(1)\fP, \fBdotty-env-add(1)\fP, \fBdotty-env-get(1)\fP, \fBdotty-env-list(1)\fP, \fBdotty-env-remove(1)\fP, \fBdotty-env-run(1)\fP, \fBdotty-env-use(1)\fP
